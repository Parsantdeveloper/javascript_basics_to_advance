let user={
    id:1,
    name:"Prashant Bhusal",
    age:18,
    course:["frontend","backend","system design",
        "DSA"
    ],
    isAdmin:true
}



console.log(`this is object ${JSON.stringify(user)}`);

console.log("",user)

// console.log(`this is json ${JSON.stringify(user)}`);

// let json=JSON.stringify(user);
// console.log("converting back to object",JSON.parse(json))